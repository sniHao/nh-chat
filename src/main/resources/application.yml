server:
  port: 8087
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nh_chat?useSSL=true&useUncode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
mybatis-plus:
  mapper-locations: classpath:mybatis/mapper/*.xml
  type-aliases-package: nh.chat.bean.po
  configuration:
    map-underscore-to-camel-case: true # Mybatis??????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ??sql

---
#redis
spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 2051911977

---
spring:
  servlet:
    multipart:
      max-file-size: 4MB
      max-request-size: 10MB

---
spring:
  datasource:
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nh_chat?useUncode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    type: com.alibaba.druid.pool.DruidDataSource
    # ????????
    druid:
      #   ???????
      # ???????????????????????
      # ???????????
      initial-size: 10
      min-idle: 10
      max-active: 20
      # ?????????????
      max-wait: 60000
      #???????????????????????
      remove-abandoned-timeout: 180
      remove-abandoned: true
      # ???????????????????????????????
      time-between-eviction-runs-millis: 60000
      # ??????????????????????
      min-evictable-idle-time-millis: 300000
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      maxPoolPreparedStatementPerConnectionSize: 20
      useGlobalDataSourceStat: true
      connectionProperties: druid.stat.mergeSql=true;druid.stat.logSlowSql=true;druid.stat.slowSqlMillis=1000;